<?xml version="1.0" encoding="utf-8" ?>
<!--
    Derived from CopyButton.xaml in the Microsoft WinUI Gallery.
    Copyright (c) Microsoft Corporation. Licensed under the MIT License.
    https://github.com/microsoft/WinUI-Gallery/blob/29f62479d5c046a0b854a5868e5a7cd484572d87/WinUIGallery/Controls/CopyButton.xaml

    Modified for Amplify:
      - CopyButtonPointerOverForegroundBrush / CopyButtonPressedForegroundBrush are Gallery-defined
        resources we do not have, so the framework ButtonForegroundPointerOver / ButtonForegroundPressed
        are used instead.
      - The AnimatedIcon.State setters are dropped: they only drive a Lottie-backed AnimatedIcon, and
        the content here is a plain Segoe Fluent glyph (as it is in the Gallery's own usage), so they
        were already inert.
      - UseSystemFocusVisuals is set literally rather than via {StaticResource UseSystemFocusVisuals};
        a StaticResource resolves at load time, so a miss would be a runtime failure the build cannot
        catch, whereas a literal cannot fail.
      - HorizontalAlignment/VerticalAlignment setters are dropped so the button aligns to whatever the
        host layout dictates (it sits in a Grid column on the onboarding screen).
      - Upstream splits this into a keyed DefaultCopyButtonStyle plus an unkeyed style BasedOn it, so
        gallery consumers can derive from it. Amplify has a single consumer and nothing derives from
        it, so it is one unkeyed style.
      - Upstream's CopyButton sets DefaultStyleKey, which is dead weight there: it points at a
        Themes/Generic.xaml the Gallery does not have, and the unkeyed style below is what actually
        templates the control. Dropped rather than copied.
    See THIRD-PARTY-NOTICES.md.

    Applied implicitly: the style is unkeyed, so it attaches to every CopyButton by TargetType,
    overriding the default Button style the control would otherwise inherit. That requires this
    dictionary to be merged into App.xaml — it is not discovered automatically.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Amplify.App.Controls">

    <Style TargetType="controls:CopyButton">
        <Setter Property="Background" Value="{ThemeResource ButtonBackground}" />
        <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
        <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource ButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{ThemeResource ButtonBorderThemeThickness}" />
        <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
        <Setter Property="FontFamily" Value="{ThemeResource SymbolThemeFontFamily}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="IsTextScaleFactorEnabled" Value="False" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="FocusVisualMargin" Value="-3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:CopyButton">
                    <Grid
                        x:Name="RootGrid"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BackgroundSizing="{TemplateBinding BackgroundSizing}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}">

                        <Grid.Resources>
                            <!--
                                Found by name from the code-behind and started on click. Timeline: the
                                content shrinks away over 133ms, the checkmark pops in (overshooting to
                                1.146 before settling), holds until 1.167s, then the two swap back.
                                Only Opacity and ScaleTransform are animated, so this stays off the UI
                                thread and needs no EnableDependentAnimation.
                            -->
                            <Storyboard x:Name="CopyToClipboardSuccessAnimation">
                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Opacity">
                                    <LinearDoubleKeyFrame KeyTime="0:0:0" Value="1.0" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.33,0.0 0.67,1.0"
                                        KeyTime="0:0:0.133"
                                        Value="0.0" />
                                    <LinearDoubleKeyFrame KeyTime="0:0:1.3" Value="0.0" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="1.0,0.0 1.0,1.0"
                                        KeyTime="0:0:1.433"
                                        Value="1.0" />
                                </DoubleAnimationUsingKeyFrames>

                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CopyToClipboardTextScaleTransform" Storyboard.TargetProperty="ScaleX">
                                    <LinearDoubleKeyFrame KeyTime="0:0:0" Value="1.0" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.13,0.0 0.0,1.0"
                                        KeyTime="0:0:0.133"
                                        Value="0.273" />
                                    <LinearDoubleKeyFrame KeyTime="0:0:1.167" Value="0.273" />
                                    <LinearDoubleKeyFrame KeyTime="0:0:1.2" Value="1.0" />
                                </DoubleAnimationUsingKeyFrames>
                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CopyToClipboardTextScaleTransform" Storyboard.TargetProperty="ScaleY">
                                    <LinearDoubleKeyFrame KeyTime="0:0:0" Value="1.0" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.13,0.0 0.0,1.0"
                                        KeyTime="0:0:0.133"
                                        Value="0.273" />
                                    <LinearDoubleKeyFrame KeyTime="0:0:1.167" Value="0.273" />
                                    <LinearDoubleKeyFrame KeyTime="0:0:1.2" Value="1.0" />
                                </DoubleAnimationUsingKeyFrames>

                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CopySuccessGlyph" Storyboard.TargetProperty="Opacity">
                                    <LinearDoubleKeyFrame KeyTime="0:0:0.133" Value="0.0" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.33,0.0 0.67,1.0"
                                        KeyTime="0:0:0.267"
                                        Value="1.0" />
                                    <LinearDoubleKeyFrame KeyTime="0:0:1.167" Value="1.0" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="1.0,0.0 0.98,1.0"
                                        KeyTime="0:0:1.3"
                                        Value="0.0" />
                                </DoubleAnimationUsingKeyFrames>

                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CopyToClipboardSuccessTextScaleTransform" Storyboard.TargetProperty="ScaleX">
                                    <LinearDoubleKeyFrame KeyTime="0:0:0.133" Value="0.385" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.39,0.0 0.63,1.0"
                                        KeyTime="0:0:0.267"
                                        Value="1.146" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.55,0.0 0.02,1.0"
                                        KeyTime="0:0:0.333"
                                        Value="1.0" />
                                </DoubleAnimationUsingKeyFrames>
                                <DoubleAnimationUsingKeyFrames Storyboard.TargetName="CopyToClipboardSuccessTextScaleTransform" Storyboard.TargetProperty="ScaleY">
                                    <LinearDoubleKeyFrame KeyTime="0:0:0.133" Value="0.385" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.39,0.0 0.63,1.0"
                                        KeyTime="0:0:0.267"
                                        Value="1.146" />
                                    <SplineDoubleKeyFrame
                                        KeySpline="0.55,0.0 0.02,1.0"
                                        KeyTime="0:0:0.333"
                                        Value="1.0" />
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                        </Grid.Resources>

                        <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </Grid.BackgroundTransition>

                        <ContentPresenter
                            x:Name="ContentPresenter"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            Foreground="{TemplateBinding Foreground}"
                            RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter.RenderTransform>
                                <ScaleTransform x:Name="CopyToClipboardTextScaleTransform" />
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>

                        <!-- The confirmation checkmark, stacked over the content and revealed by the animation. -->
                        <ContentPresenter
                            x:Name="CopySuccessGlyph"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Foreground="{TemplateBinding Foreground}"
                            Opacity="0"
                            RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter.RenderTransform>
                                <ScaleTransform x:Name="CopyToClipboardSuccessTextScaleTransform" />
                            </ContentPresenter.RenderTransform>
                            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="16" Glyph="&#xE73E;" />
                        </ContentPresenter>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonBackgroundPointerOver}" />
                                        <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonBorderBrushPointerOver}" />
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ButtonForegroundPointerOver}" />
                                        <Setter Target="CopySuccessGlyph.Foreground" Value="{ThemeResource ButtonForegroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonBackgroundPressed}" />
                                        <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonBorderBrushPressed}" />
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ButtonForegroundPressed}" />
                                        <Setter Target="CopySuccessGlyph.Foreground" Value="{ThemeResource ButtonForegroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="RootGrid.Background" Value="{ThemeResource ButtonBackgroundDisabled}" />
                                        <Setter Target="RootGrid.BorderBrush" Value="{ThemeResource ButtonBorderBrushDisabled}" />
                                        <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ButtonForegroundDisabled}" />
                                        <Setter Target="CopySuccessGlyph.Foreground" Value="{ThemeResource ButtonForegroundDisabled}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
